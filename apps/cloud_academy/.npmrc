# Suppress deprecation warnings from transitive dependencies
# These warnings come from old dependencies used by Next.js/ESLint
# and don't affect functionality
legacy-peer-deps=true
fund=false
audit=false

